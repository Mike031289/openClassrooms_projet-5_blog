<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Liste des posts</title>
</head>
<body>
    <h1>Liste des posts</h1>
    {% if posts is empty %}
        <p>Aucun post disponible.</p>
    {% else %}
        <ul>
            {% for post in posts %}
                <li>
                    <h2>{{ post.title }}</h2>
                    <p>{{ post.content }}</p>
                    <p>Auteur : {{ post.author }}</p>
                    <p>Date de publication : {{ post.published_at|date("d/m/Y") }}</p>
                </li>
            {% endfor %}
        </ul>
    {% endif %}
</body>
</html>
