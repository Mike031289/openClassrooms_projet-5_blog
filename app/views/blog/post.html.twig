
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>{{ title }}</title>
</head>
<body>
    <h1>Liste des posts</h1>
    {% if posts is empty %}
        <p>Aucun post disponible.</p>
    {% else %}
        <ul>
            {% for post in posts %}
                <li>
                    <h2>{{ post.title }}</h2>
                    <img src="{{ post.imageUrl}}" alt="Image">
                    <p>{{ post.content }}</p>
                    <p>Auteur : {{ post.authorId}}</p>
                    <p>Date de publication: {{ post.createdAt}}</p>
                    <p>Date de mise à jour: {{ post.updatedAt}}</p>
                </li>
            {% endfor %}
        </ul>
    {% endif %}
    
</body>
</html>

