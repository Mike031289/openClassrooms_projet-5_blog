{% extends 'templates/layout.html.twig' %}

{% block header %}
    <header class="py-5 bg-light border-bottom mb-4">
        <div class="container">
            <div class="text-center my-5">
                <h1>{{ post.title }}</h1>
                <p class="lead mb-0">Nous vous informons sur tous nos activités</p>
            </div>
        </div>
    </header>
{% endblock %}

{% block content %}
<div class="container">
    <div class="row">
        <div class="col-lg-8">
            {% if post %}
                <div class="card mb-4 p-0">
                    <a href="#!">
                        <img class="card-img-top" src="https://dummyimage.com/700x350/dee2e6/6c757d.jpg" alt="..." />
                    </a>
                    <div class="card-body">
                        <div class="small text-muted">Date de publication: {{ post.createdAt }}</div>
                        <div class="small text-muted">Date de mise à jour: {{ post.updatedAt }}</div>
                        <p class="card-text">{{ post.content }}</p>
                        <p>Auteur : {{ post.authorId }}</p>
                    </div>
                </div>
            {% else %}
                <p>L'article n'existe pas.</p>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}
